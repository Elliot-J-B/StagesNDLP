{% extends 'base.html.twig' %}

{% block title %}Entreprise index{% endblock %}

{% block body %}

<p id="home"></p> {# Ancre pour le lien vers l'accueil #}
<nav class="navbar fixed-top navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <img src="/images/logo.jpeg" alt="Logo" width="35" height="35" {# Logo de l'entreprise#}
      class="d-inline-block align-text-top">
      <a class="navbar-brand"></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav w-100">
        <a class="nav-link" href="/">Accueil</a> {# Redirige vers l'accueil #}
        <a class="nav-link" href="/dashboard">Dashboard</a> {# Redirige vers la section comment ça marche ? #}
        <a class="nav-link active" aria-current="page" href="/entreprise">Gestion Entreprises / Stages</a> {# Redirige vers la page de recherches d'entreprises#}
        <a class="nav-link" href="/prof">Gestion Roles</a>
      </div>
    </div>
  </div>
</nav>

<br>
<br>
<br>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Email</th>
                <th>Image</th>
                <th>Numero</th>
                <th>Descriptif</th>
                <th>Localisation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entreprise in entreprises %}
            <tr>
                <td>{{ entreprise.id }}</td>
                <td>{{ entreprise.nom }}</td>
                <td>{{ entreprise.email }}</td>
                <td><img src="{{ entreprise.image }}" alt="Image" width="100"></td>
                <td>{{ entreprise.numero }}</td>
                <td>{{ entreprise.descriptif }}</td>
                <td>{{ entreprise.localisation }}</td>
                <td>
                    <a href="{{ path('app_entreprise_show', {'id': entreprise.id}) }}">show</a>
                    <a href="{{ path('app_entreprise_edit', {'id': entreprise.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_entreprise_new') }}">Create new</a>
{% endblock %}
