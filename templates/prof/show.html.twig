{% extends 'base.html.twig' %}

{% block title %}Prof{% endblock %}

{% block body %}
    <h1>Prof</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prof.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ prof.username }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ prof.roles ? prof.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ prof.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_prof_index') }}">back to list</a>

    <a href="{{ path('app_prof_edit', {'id': prof.id}) }}">edit</a>

    {{ include('prof/_delete_form.html.twig') }}
{% endblock %}
